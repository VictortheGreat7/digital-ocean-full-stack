apiVersion: v1
kind: Service
metadata:
  name: kronos-postgres-svc
  namespace: kronos
spec:
  selector:
    app: kronos-app
    component: db
    environment: development
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
