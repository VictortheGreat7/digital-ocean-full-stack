provider:
  name: cloudflare
sources:
  - gateway-httproute
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-token
        key: api-token
policy: sync
txtOwnerId: "kronos"
extraArgs:
  - --gateway-namespace=kube-system
  - --cloudflare-proxied